# chromatic-theory (`coq-chromatic-theory` / `rocq-chromatic-theory`)

Area package of `graph-theory-rocq`. Namespace `Chromatic`. Phases: U1, U4, U5, U8.
Conjectures: **32 core**
(see `meta/opg_corpus_manifest.json`, filter `repo==chromatic-theory`).

Layout (the digraph-theory template): `theories/{foundations,core,invariants,constructions,conjectures,applications}/`.
Depends only on `base/` (+ `digraph-theory/` if directed). **Status: scaffold — milestone pending.**
