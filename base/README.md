# graph-theory-base (`coq-graph-theory-base`)

The single owner of **cross-area primitives** (plan §A ownership table): the undirected interop
façade (re-exports `coq-graph-theory` ω/α/χ/clique/core-minor/treewidth/dom), graph homomorphism
`hom`, graph products (tensor/cartesian/lex), list-assignment + list-χ, line-graph + total-graph, Δ.
Planarity façade is added only after the fourcolor spike (gate G2).

**Status: scaffold (gate G3 pending).** Area packages depend ONLY on this — never on each other.
