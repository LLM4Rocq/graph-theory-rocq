# infinite-graph-theory (`coq-infinite-graph-theory` / `rocq-infinite-graph-theory`)

Area package of `graph-theory-rocq`. Namespace `Infinite`. Phases: D4.
Conjectures: **0 core** + 14 deferred
(see `meta/opg_corpus_manifest.json`, filter `repo==infinite-graph-theory`).

Layout (the digraph-theory template): `theories/{foundations,core,invariants,constructions,conjectures,applications}/`.
Depends only on `base/` (+ `digraph-theory/` if directed). **Status: PARKED (deferred tail; opt-in).**
